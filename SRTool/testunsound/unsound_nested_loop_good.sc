void main()
{
	int start;
	int i;
	int j;
	i=start;
	j = start;
	while(i)
	bound(2)
	{
		i = i - 1;

		while(j)
		bound(2)
		{
		    j = j - 1;
		}

	}

	// without unwinding assertions, this assert failure should be missed

	if(start > 2)
	{
		assert(0);
	}

}

